
import 'package:flutter/material.dart';

/// Light Theme
const klBackgroundColor = Color(0xffffffff); // White for a clean, bright background
const klCanvasColor = Color(0xffe0f2f1); // Light teal for subtle contrast
const klPageBackgroundColor = Color(0xffe8f5e9); // Very light green for page backgrounds
const klPrimaryColor = Color(0xff00796b); // Teal as the main color
const klSecondaryColor = Color(0xffffb300); // Amber as secondary accent color
const klPrimaryTextColor = Color(0xff212121); // Dark text color for readability
const klSecondaryTextColor = Color(0xff616161); // Dark gray for less emphasis
const klHintTextColor = Color(0xff757575); // Gray for hint texts
const klDisabledTextColor = Color(0xff9e9e9e); // Light gray for disabled states
const klListItemBackgroundColor = Color(0xfff5f5f5); // Light gray for list items

/// Dark Theme (Fixed Black-on-Black Issue)
const kdBackgroundColor = Color(0xff121212); // Dark slate background
const kdCanvasColor = Color(0xff252525); // Slightly lighter dark gray for contrast
const kdPageBackgroundColor = Color(0xff1e1e1e); // Dark gray for better separation
const kdPrimaryColor = Color(0xff00796b); // Teal as the main color
const kdSecondaryColor = Color(0xffffb300); // Amber as secondary accent color
const kdPrimaryTextColor = Color(0xffffffff); // **Fixed: White text for high contrast**
const kdSecondaryTextColor = Color(0xffb0bec5); // **Fixed: Light gray for better visibility**
const kdHintTextColor = Color(0xff90a4ae); // **Fixed: Medium gray for hints**
const kdDisabledTextColor = Color(0xff616161); // **Fixed: Dark gray for disabled states**
const kdListItemBackgroundColor = Color(0xff292929); // Dark gray for list items

/// Common Colors
const kAddCoinColor = Color(0xff4caf50); // Green for positive actions
const kBadgeLockedColor = Color(0xff9e9e9e); // Neutral grey for locked states
const kHurryUpTimerColor = Color(0xfff44336); // Red to signify urgency
const kCorrectAnswerColor = Color(0xff388e3c); // Dark green for correct answers
const kWrongAnswerColor = Color(0xffe53935); // Red for incorrect answers
const kPendingColor = Color(0xffffa726); // Warm orange for pending states